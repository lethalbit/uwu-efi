# SPDX-License-Identifier: BSD-3-Clause
uwuefi_arch_amd64_inc = include_directories('.')

uwuefi_arch_amd64_headers = files([

])

install_headers(
	uwuefi_arch_amd64_headers,
	subdir: 'uwu-efi' / 'arch' / 'amd64'
)

libuwuefi_srcs += files([

])
