# SPDX-License-Identifier: BSD-3-Clause
uwuefi_arch_ebc_inc = include_directories('.')

uwuefi_arch_ebc_headers = files([

])

install_headers(
	uwuefi_arch_ebc_headers,
	subdir: 'uwu-efi' / 'arch' / 'ebc'
)

libuwuefi_srcs += files([

])
