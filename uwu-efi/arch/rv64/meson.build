# SPDX-License-Identifier: BSD-3-Clause
uwuefi_arch_rv64_inc = include_directories('.')

uwuefi_arch_rv64_headers = files([

])

install_headers(
	uwuefi_arch_rv64_headers,
	subdir: 'uwu-efi' / 'arch' / 'rv64'
)

libuwuefi_srcs += files([

])
