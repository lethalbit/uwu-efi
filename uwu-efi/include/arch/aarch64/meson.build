# SPDX-License-Identifier: BSD-3-Clause
uwuefi_arch_aarch64_inc = include_directories('.')

uwuefi_arch_aarch64_headers = files([

])

install_headers(
	uwuefi_arch_aarch64_headers,
	subdir: 'uwuefi' / 'arch' / 'aarch64'
)

